export const regexList = {
  forAuth: /^([^\s@]+@[^\s@]+\.[^\s@]+|[VEve]\d+)$/,
  onlyString: /^\D+$/,
  forDir: /^[A-Za-z0-9\s\\.\\-]+$/,
  forDNI: /^[VEve]\d+$/,
  forRelation: /^[A-Za-z]+$/,
  forToken: /^[A-Za-z0-9]+$/,
};
